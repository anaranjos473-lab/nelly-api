# CONTINUAR AQUI - Nelly (2026-07-13)

## Punto de reanudacion

El flujo operativo principal y el fallback de evidencia del repartidor quedaron certificados en un Motorola real. No reabrir refactorizaciones ni cambiar el contrato de estados. Tratar cualquier hallazgo nuevo como incidente pequeno y aislado.

La fase activa ahora es C5.2-B.1 - Ventana controlada. Leer `C5_2_B_1_VENTANA_OBSERVACION.md`. El usuario ya configuro personalmente el flag y el identificador de observacion, pero la ventana NO inicio: Render esta ejecutando `node app.js` y el Shadow solo se inicia desde `server.js`. No registrar T0 hasta corregir el comando de arranque y comprobar los eventos `initial_metrics` y `enabled`. C4, productores V2 y C5.2-C siguen bloqueados.

El usuario siempre introduce personalmente credenciales, claves y datos de acceso. No pedirlos, leerlos, copiarlos ni escribirlos por el usuario.

## Repositorios

### Web/backend

- Ruta: `C:\Users\hp14\OneDrive\Desktop\nelly`
- Rama: `main`
- Commit desplegado y verificado en Render: `aaeb653 Tag C5 shadow metrics by observation`
- Estado: limpio y sincronizado con `origin/main` antes de actualizar este documento.
- Fallback web ya versionado en:
  - `public/evidence-fallback.js`
  - `public/subirEvidencia.js`
  - `tests/evidence-fallback.test.mjs`
  - `storage.rules`

### Android repartidor

- Ruta: `C:\Users\hp14\AndroidStudioProjects\NellyDriver`
- Rama: `master`
- Ultimo incremento C4 publicado: `904cf2c Add C4 dynamic route destination`
- Otros commits funcionales relevantes:
  - `be5793d Validate offers against canonical orders`
  - `370c168 Add Android evidence upload fallback`
- Estado: limpio y sincronizado con `origin/master`.
- APK corregida instalada en el Motorola conectado.

## Certificacion realizada

Se probaron dos entregas independientes con fotografias tomadas y confirmadas personalmente por el usuario:

1. `CERT_EVIDENCIA_FALLBACK_1783934413094`
   - Estado canonico: `ENTREGADO`
   - Evidencia: JPEG Base64 valida
   - Longitud registrada: 11247
   - `evidencia_fallback=true`

2. `CERT_EVIDENCIA_FALLBACK_1783935184194`
   - Estado canonico: `ENTREGADO`
   - Evidencia: JPEG Base64 valida
   - Longitud registrada: 10571
   - `evidencia_fallback=true`

Las marcas de tiempo son distintas y el segundo cierre no sobrescribio la evidencia del primero. La app regreso al radar despues de finalizar.

## Incidente encontrado y corregido

Durante la segunda prueba, una copia obsoleta `LISTO` en `pedidos_para_reparto` hizo que una version anterior reutilizara el primer pedido entregado. La correccion Android ahora valida toda oferta contra `pedidos/{id}`, que es la fuente canonica, y rechaza espejos cuyo pedido canonico ya no esta disponible.

Las copias obsoletas de los dos pedidos de certificacion fueron retiradas de `pedidos_para_reparto`. Los registros canonicos y sus evidencias se conservaron.

## Estado real y pendiente

- Fallback Android: certificado dos veces en dispositivo real.
- Fallback web: implementado, probado y versionado.
- Mapa Android y filtrado de pedidos residuales: corregidos y validados previamente.
- Firebase Storage nativo: sigue pendiente como infraestructura. Actualmente falla y activa correctamente el fallback Base64.
- Algunos espejos operativos pueden devolver `Permission denied`; `pedidos/*` permanece como fuente canonica y la app ya se protege contra indices obsoletos.
- C4 ya tiene mapa, politica de geocerca y cambio dinamico de ruta implementados en Android, pero no puede certificarse con el productor oficial actual porque faltan coordenadas.
- C5 documento dos productores API oficiales incompatibles, productores directos de prueba, consumidores con alias distintos y una fuerte deriva en los datos historicos.
- La auditoria C5 esta completa, pero las cuatro casillas de cumplimiento siguen en FAIL. No confundir auditoria terminada con contrato certificado.
- C5.1 separa contrato, maquina de estados, fases y eventos en documentos distintos. Ninguno esta aprobado por el solo hecho de existir.
- Las nueve decisiones estructurales, inventario, migracion y rollback quedaron validados documentalmente. Eso no demuestra compatibilidad del runtime.
- C5.2-A confirmo que los consumidores toleran partes del V2, pero ninguno puede operar integralmente con el contrato sin adaptacion.
- C5.2-B agrego validacion pura, metricas agregadas y observador RTDB de solo lectura bajo `ENABLE_C5_SHADOW_VALIDATOR=true`. El usuario configuro personalmente `ENABLE_C5_SHADOW_VALIDATOR=true` y `C5_SHADOW_OBSERVATION_ID=B1-2026-07`.
- Render confirmo el deploy live de `aaeb653`, y los endpoints `/api/health` y `/api/salud` respondieron correctamente en produccion.
- Incidente de configuracion actual: los logs muestran `==> Running 'node app.js'`. Ese punto de entrada no llama `iniciarC5ShadowValidator()`. `render.yaml` y `package.json` ya indican `node server.js`, por lo que existe un override manual del Start Command en Render.
- No aparecieron logs `[C5_SHADOW]`; por tanto B.1 no esta activa y T0 no fue registrado. No contar pedidos ni horas de observacion todavia.
- B.1 cerrara al cumplir 72 horas y 25 pedidos nuevos, o al llegar a 7 dias; tiene umbrales de aborto y un unico informe agregado.
- El informe incluira `pedidos V2 validos / total cohorte x 100`; el porcentaje prioriza adaptadores, pero no es un umbral automatico que bloquee C5.2-C.

## Como continuar

1. Leer este archivo, `C5_AUDITORIA_CONTRATO_DATOS.md` y comprobar ambos repositorios con `git status --short --branch`.
2. Dejar que el usuario introduzca siempre los accesos.
3. En Render, el usuario debe cambiar unicamente el Start Command de `node app.js` a `node server.js`, guardar y reiniciar/desplegar. No pedir, leer ni manipular sus accesos.
4. Validar en los logs: `Running 'node server.js'`, `[C5_SHADOW]` con `event=initial_metrics`, `[C5_SHADOW]` con `event=enabled`, `observation_id=B1-2026-07` y ausencia de errores del listener.
5. Solo entonces registrar T0 usando la hora real de `initial_metrics` y comenzar a contar la ventana. Desactivar ante cualquier umbral del protocolo.
6. No migrar datos historicos ni inventar coordenadas; primero definir respaldo y politica de compatibilidad.
7. Despues de migrar productores y consumidores, retomar C4 con un pedido oficial nacido con ambas ubicaciones.

## Herramientas temporales utiles

Los scripts locales ignorados en `.codex-tmp` permiten inspeccionar y limpiar solamente pedidos de certificacion:

- `inspect-evidence-fallback-order.cjs`
- `inspect-pedido-fields.cjs`
- `cleanup-entregado-pool.cjs`
- `publish-evidence-fallback-order.cjs`

No imprimir ni compartir el contenido Base64 completo de una evidencia; validar solo tipo, longitud, prefijo y metadatos.

## Criterio de cierre de esta etapa

C5.1, C5.2-A, C5.2-B y el diseño B.1 estan aprobados. B.1 espera corregir el Start Command y validar sus logs; todavia no tiene T0. Al cerrar se genera un informe unico; no habilitar productores V2, iniciar C5.2-C ni reanudar C4.
