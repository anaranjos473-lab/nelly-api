{
  "rules": {
    "repartidores_activos": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.exists()"
      }
    },
    "conductores_activos": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.exists()"
      }
    },
    "seguridad": {
      "alertas_panico": {
        ".write": "auth != null",
        ".read": "false"
      }
    }
  }
}
